# Data Model

This document describes the core data entities and their relationships.

Claude should consult this document when working with database schemas, queries, or domain models.

<!-- TODO: Fill in with actual data model once the Clarity codebase is set up -->

---

## Sections to Define

- Core entities and their fields
- Relationships between entities
- Key constraints and indexes
- Soft delete strategy (if any)
- Audit fields (created_at, updated_at, etc.)
- Sensitive fields that require special handling
- Migration strategy
